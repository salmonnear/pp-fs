{
  "name": "pp-fs",
  "version": "1.0.0",
  "description": "rest framework for pp factory app",
  "engines": {
    "node": "10.13.0"
  },
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "author": "Sam Mennear",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongoose": "^5.6.9",
    "morgan": "^1.9.1"
  }
}
